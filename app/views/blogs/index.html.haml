%p#notice= notice
%h1 Listing Blogs
%table
  %thead
    %tr
      %th Title
      %th Subtitle
      %th Body

  %tbody
    = render @blogs

%br/
= link_to "New Blog", new_blog_path if logged_in?(:site_admin)