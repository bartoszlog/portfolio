%p#notice= notice
%h1 Listing Blogs
%table
  %thead
    %tr
      %th Title
      %th Body
      %th{:colspan => "3"}
  %tbody
    = render @portfolios
= link_to 'Create new portfolio', new_portfolio_path if logged_in?(:site_admin)